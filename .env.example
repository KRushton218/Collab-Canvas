# Firebase Configuration Template
# Get these values from your Firebase Console: https://console.firebase.google.com/
# Project Settings > General > Your apps > Firebase SDK snippet > Config

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_REALTIME_DATABASE_URL=https://your_project_id-default-rtdb.firebaseio.com

# ============================================================================
# ENVIRONMENT-SPECIFIC FILES (Backend Separation)
# ============================================================================
#
# .env.production  → Production backend (collab-canvas-ed2fc)
#                    Used by: npm run build, npm run firebase:deploy:prod
#
# .env.development → Dev backend (collab-canvas-dev)
#                    Used by: npm run build:dev, npm run firebase:deploy:dev
#
# .env.local       → Local emulator (optional)
#                    Used by: npm run dev (if VITE_USE_EMULATOR=true)
#
# Vite automatically loads the correct .env file based on --mode flag
# See: https://vitejs.dev/guide/env-and-mode.html
